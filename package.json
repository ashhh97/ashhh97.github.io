{
  "name": "yzportfolio2025",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "analyze": "vite build --mode analyze",
    "optimize-images": "node scripts/optimize-images.js",
    "compress-videos": "node scripts/compress-videos.js",
    "optimize-all": "npm run optimize-images && npm run compress-videos",
    "build-optimized": "npm run optimize-all && npm run build",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "gh-pages": "^6.3.0",
    "rollup-plugin-visualizer": "^5.12.0",
    "sharp": "^0.34.3",
    "terser": "^5.43.1",
    "vite": "^7.1.0",
    "vite-plugin-compression": "^0.5.1"
  }
}
